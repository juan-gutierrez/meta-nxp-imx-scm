From 189ffe1c2e971816d04f55ec11a1bd4b88f4af47 Mon Sep 17 00:00:00 2001
From: Alejandro Sierra <b18039@freescale.com>
Date: Mon, 18 Apr 2016 15:32:26 -0500
Subject: [PATCH 08/23] Add Micrel phy init for imx6sx

Add Micrel phy initialization for imx6sxscm evb platform

Signed-off-by: Alejandro Sierra <b18039@freescale.com>
---
 arch/arm/mach-imx/mach-imx6sx.c | 25 +++++++++++++++++++++++++
 1 file changed, 25 insertions(+)

diff --git a/arch/arm/mach-imx/mach-imx6sx.c b/arch/arm/mach-imx/mach-imx6sx.c
index eda08df..d88122d 100755
--- a/arch/arm/mach-imx/mach-imx6sx.c
+++ b/arch/arm/mach-imx/mach-imx6sx.c
@@ -21,6 +21,7 @@
 #include <linux/netdevice.h>
 #include <asm/mach/arch.h>
 #include <asm/mach/map.h>
+#include <linux/micrel_phy.h>
 
 #include "common.h"
 #include "cpuidle.h"
@@ -170,6 +171,28 @@ static int __init imx6sx_arm2_flexcan_fixup(void)
 	return 0;
 }
 
+static void mmd_write_reg(struct phy_device *dev, int device, int reg, int val)
+{
+	phy_write(dev, 0x0d, device);
+	phy_write(dev, 0x0e, reg);
+	phy_write(dev, 0x0d, (1 << 14) | device);
+	phy_write(dev, 0x0e, val);
+}
+
+static int ksz9031rn_phy_fixup(struct phy_device *dev)
+{
+	/*
+	 * min rx data delay, max rx/tx clock delay,
+	 * min rx/tx control delay
+	 */
+	mmd_write_reg(dev, 2, 4, 0);
+	mmd_write_reg(dev, 2, 5, 0);
+	mmd_write_reg(dev, 2, 8, 0x003ff);
+
+	return 0;
+}
+
+
 static int ar8031_phy_fixup(struct phy_device *dev)
 {
 	u16 val;
@@ -198,6 +221,8 @@ static int ar8031_phy_fixup(struct phy_device *dev)
 static void __init imx6sx_enet_phy_init(void)
 {
 	if (IS_BUILTIN(CONFIG_PHYLIB))
+		phy_register_fixup_for_uid(PHY_ID_KSZ9031, MICREL_PHY_ID_MASK,
+				ksz9031rn_phy_fixup);
 		phy_register_fixup_for_uid(PHY_ID_AR8031, 0xffffffff,
 					   ar8031_phy_fixup);
 }
-- 
1.9.1

